# 大种群绘制优化

成虫达到 64 只时启用后台动画图集。一个 Web Worker 复用现有矢量绘制代码，为普通颜色、性别和动作生成图集；主线程只负责旋转、缩放和贴图。少量成虫仍使用原绘制路径，彩虹果蝇保留实时变色。

任务按使用次数和等待时间排序，同一种图集只排队一次，每次只执行一个任务。最多保留 30 张 1024×64 图集，原始像素约 7.5 MiB（不含浏览器/GPU额外开销）。Worker 失败或任务超时回退到矢量绘制，页面销毁时释放线程及图像。动画相位从 8 帧细化为 16 帧，位置与方向仍逐帧更新。

## CPU 核心池

托盘的“自动性能优化”默认开启（本次运行有效），成虫达到 64 只时通过 Windows CPU Sets 设置进程级软亲和性。识别物理核、逻辑线程、NUMA、处理器组及 EfficiencyClass；同一物理核的可用超线程保持在同一池中。按最近 10 秒的 CPU 时间增量估算负载，兼顾性能核心等级，为渲染进程选择约 75% 的可用物理核（最多 8 核），主进程使用另一个物理核。Worker 共享渲染进程核心池，并未单独绑定其线程。

不是独占核心，也不修改系统或其他应用设置。每分钟最多重分配一次，且要求候选渲染池负载降低至少 20 个百分点；核心停驻或不可用时提前重选。小于 4 个可用物理核、多处理器组或多 NUMA 节点的复杂拓扑交由系统调度；已有外部 CPU Sets 策略会保留。接口失败后回退，关闭开关、数量回落或退出时恢复原 CPU Sets，硬亲和性掩码不修改。

本机实际识别 6 核 / 12 线程，同构核心。测试分配为渲染池 10 个逻辑线程、主进程池 2 个逻辑线程，赋值后回读一致，释放后原设置恢复。大小核策略及保守回退使用模拟拓扑测试，尚未在对应硬件实测。

实现依据：[Windows CPU Sets](https://learn.microsoft.com/en-us/windows/win32/procthread/cpu-sets)、[拓扑结构](https://learn.microsoft.com/en-us/windows/win32/api/winnt/ns-winnt-system_cpu_set_information)。软亲和性仍由 Windows 最终调度，不能保证每个 Chromium 线程严格绑定，已有线程级策略优先。

## GPU 与帧时间

本机 Electron 报告 Canvas、GPU 合成、光栅化、多光栅线程均已开启，继续采用默认硬件加速路径，不添加绕过驱动兼容性的实验性开关。生产绘制路径不逐帧回读像素。

高 DPI 下持续 90 帧超过约 22 ms 帧间隔或 18 ms 前台工作时间时，画布内部像素比例每次降低 0.15，最低一 CSS 像素对应一背板像素；稳定 300 帧后每次恢复 0.1。调整间隔至少 5 秒，长时间挂起的帧忽略。拿工具、庆祝文字、小种群或关闭优化时恢复完整精度。它减少高分屏像素负担，以适量清晰度换取稳定性；普通 100% 缩放不降低精度。当前阈值以约 60 Hz 为目标，不是显卡利用率控制器，也未实现显示器刷新率自适应。

生命周期模拟仍在主线程，桌面图标轮询等开销不在这次优化范围内。

## 复测

安装依赖后运行 `npx electron scripts/benchmark-render.mjs`。测试使用独立隐藏窗口和独立用户目录，不会启动游戏或改写游戏存档。

本机 1920×1080 画布，普通颜色飞行成虫，预热 10 帧、采样 20 帧，包含强制像素回读。单次结果：

| 成虫数 | 原矢量绘制中位数 | 图集绘制中位数 | 原 P95 | 图集 P95 |
| --- | --- | --- | --- | --- |
| 12 | 1.6 ms | 15.5 ms | 2.5 ms | 18.9 ms |
| 192 | 19.6 ms | 16.7 ms | 22.5 ms | 18.9 ms |
| 384 | 41.4 ms | 18.2 ms | 62.6 ms | 21.7 ms |

本轮使用 16 帧图集且启用了同一套核心池策略，30 组图集预热约 64 ms，资源释放检查通过。矢量和图集两条路径使用同一调度策略，不能用这份数据单独推断 CPU 绑定收益。小种群图集路径在回读测试中反而更慢，因此生产代码在 64 只以下保持原路径。64 是暂定切换点，不是经过全量设备测量的最优阈值。

运行 `node --test scripts/performance.test.mjs` 检查核心选择、超线程归组、回退与分辨率迟滞策略。

这是绘图基准，不是完整游戏 FPS：强制回读会影响 GPU 管线，结果也受硬件和驱动影响。仍需要在实际桌面验证 192 只时的操作体验；大量彩虹果蝇、幼虫或残迹不在本次缓存范围内。
