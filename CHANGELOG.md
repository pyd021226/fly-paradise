# 更新记录

## 未发布

整理控制面板的原有功能，用生命周期概览和颜色网格替代连续长文本，并把观察、清理、种群操作和设置分区显示。统计只保留常规分类，不增加额外的目标或计时提示。抹布改成带褶皱、交织纤维、缝线边缘和阴影的布料外观，擦拭时会有轻微挤压形变。

修复拿着苍蝇拍或抹布时最小化、再打开面板后无法点击的问题。最小化时工具保持启用，可以继续在桌面沉浸操作；重新打开面板时会恢复窗口层级和焦点，让面板正常点击。控制面板改成紧凑双列按钮，默认尺寸下无需纵向滚动；缩小窗口时显示透明轨道、圆头胶囊形的细滚动条。

增加“自动性能优化”托盘开关。数量较多时按物理核与负载选择 CPU 核心池，高分屏持续掉帧时适当降低画布精度，稳定后恢复。图集动画细化到 16 帧。CPU 策略关闭后会恢复原设置；效果和硬件有关，可随时关闭对比。

大种群增加了后台动画图集：64 只及以上的普通成虫会复用 Worker 预绘制的图像，减少每帧重复画翅膀和身体的开销。线程失败时沿用原绘制方式。基准数据和限制见 PERFORMANCE.md。

这次补上了之前一直缺着的系统托盘。现在关掉或最小化控制面板不会把果蝇一起赶走，程序会安静地留在托盘里；点一下图标就能把面板叫回来。

托盘右键菜单也放了几个常用开关，包括暂停、透视、快进、苍蝇拍和抹布。要彻底退出，仍然可以从控制面板或托盘菜单操作，避免误关窗口后丢掉正在养的这一窝。

顺手把托盘相关代码单独拆成了模块，并把控制面板和托盘的操作收口到同一套处理逻辑。后面再加托盘选项时，不需要继续往主进程入口里塞代码。
